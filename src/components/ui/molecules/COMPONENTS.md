# Molecules Components Reference

Полный справочник по всем компонентам библиотеки molecules.

## Компоненты

### 📊 DataTable

**Файл**: `DataTable.tsx` (217 строк)

**Назначение**: Универсальная таблица данных с сортировкой и состояниями загрузки.

**Ключевые возможности**:
- Сортировка по колонкам
- Loading skeleton
- Empty state
- Кастомный рендеринг ячеек
- Обработка кликов по строкам
- Полная типизация данных

**Props**:
```typescript
interface DataTableProps<T> {
  columns: Column<T>[];          // Определения колонок
  data: T[];                     // Данные
  loading?: boolean;             // Состояние загрузки
  emptyMessage?: string;         // Сообщение для пустого состояния
  onSort?: (column, direction) => void;  // Обработчик сортировки
  sortColumn?: keyof T | string; // Текущая колонка сортировки
  sortDirection?: 'asc' | 'desc'; // Направление сортировки
  onRowClick?: (row: T) => void; // Обработчик клика по строке
  rowKey: keyof T;               // Ключ для идентификации строки
}
```

**Оптимизация**: useCallback для обработчиков

---

### 📄 Pagination

**Файл**: `Pagination.tsx` (93 строки)

**Назначение**: Универсальная пагинация с настройкой количества элементов.

**Ключевые возможности**:
- Переключение страниц
- Изменение количества элементов на странице
- Отображение общего количества
- Локализация на русский язык

**Props**:
```typescript
interface PaginationProps {
  page: number;                  // Текущая страница (0-based)
  totalPages: number;            // Общее количество страниц
  onPageChange: (page) => void;  // Обработчик смены страницы
  rowsPerPage: number;           // Элементов на странице
  onRowsPerPageChange: (rows) => void; // Обработчик изменения количества
  rowsPerPageOptions?: number[]; // Опции для выбора
  totalCount?: number;           // Общее количество элементов
}
```

**Оптимизация**: React.memo

---

### 🗂️ FilterDrawer

**Файл**: `FilterDrawer.tsx` (152 строки)

**Назначение**: Drawer для размещения фильтров с кнопками действий.

**Ключевые возможности**:
- Открывается справа
- Кнопки "Применить", "Сбросить", "Отмена"
- Прокручиваемое содержимое
- Автоматическая организация в Stack

**Props**:
```typescript
interface FilterDrawerProps {
  open: boolean;                 // Состояние открытия
  onClose: () => void;           // Обработчик закрытия
  onApply: () => void;           // Обработчик применения
  onReset: () => void;           // Обработчик сброса
  children: ReactNode;           // Содержимое (фильтры)
  title?: string;                // Заголовок
}
```

**Оптимизация**: React.memo

---

### 🔍 SearchField

**Файл**: `SearchField.tsx` (107 строк)

**Назначение**: Поле поиска с debounce для оптимизации запросов.

**Ключевые возможности**:
- Debounce для задержки запросов
- Иконка поиска
- Мгновенное отображение ввода
- Настраиваемая задержка

**Props**:
```typescript
interface SearchFieldProps {
  value: string;                 // Значение поиска
  onChange: (value) => void;     // Обработчик (с debounce)
  placeholder?: string;          // Placeholder
  debounceMs?: number;           // Задержка (default: 300ms)
  fullWidth?: boolean;           // Полная ширина
}
```

**Оптимизация**: useCallback, useMemo, debounce

---

### 🏷️ StatusChip

**Файл**: `StatusChip.tsx` (102 строки)

**Назначение**: Chip для отображения статуса с автоматическим маппингом цветов.

**Ключевые возможности**:
- Предустановленные статусы с цветами
- Кастомные статусы
- Локализация
- Компактный размер

**Props**:
```typescript
interface StatusChipProps {
  status: string;                // Статус
  label?: string;                // Кастомный текст
}
```

**Поддерживаемые статусы**:
- `active` → Зеленый (Активен)
- `blocked` → Красный (Заблокирован)
- `pending` → Желтый (Ожидание)
- `inactive` → Серый (Неактивен)
- `draft` → Синий (Черновик)
- `published` → Зеленый (Опубликовано)
- `archived` → Серый (Архивировано)

**Оптимизация**: React.memo, useMemo

---

### 📝 FormField

**Файл**: `FormField.tsx` (156 строк)

**Назначение**: Обертка для полей формы с интеграцией react-hook-form.

**Ключевые возможности**:
- Интеграция с Controller
- Автоматический выбор компонента по типу
- Валидация
- Поддержка всех типов полей

**Props**:
```typescript
interface FormFieldProps {
  name: string;                  // Имя поля
  control: Control<any>;         // Control из useForm
  label: string;                 // Метка
  type?: 'text' | 'password' | 'email' | 'number' |
         'select' | 'switch' | 'checkbox'; // Тип поля
  required?: boolean;            // Обязательное
  options?: SelectOption[];      // Для select
  disabled?: boolean;            // Неактивно
  placeholder?: string;          // Placeholder
  multiline?: boolean;           // Многострочное (для text)
  rows?: number;                 // Строк (для multiline)
}
```

**Поддерживаемые типы**:
- `text`, `password`, `email`, `number` → TextField
- `select` → Select
- `switch` → Switch
- `checkbox` → Checkbox

---

### ⏳ LoadingOverlay

**Файл**: `LoadingOverlay.tsx` (63 строки)

**Назначение**: Overlay с индикатором загрузки для блокировки UI.

**Ключевые возможности**:
- Backdrop с затемнением
- Индикатор загрузки
- Опциональное сообщение
- Высокий z-index

**Props**:
```typescript
interface LoadingOverlayProps {
  loading: boolean;              // Состояние загрузки
  message?: string;              // Сообщение
}
```

**Оптимизация**: React.memo

---

### 📭 EmptyState

**Файл**: `EmptyState.tsx` (103 строки)

**Назначение**: Компонент для отображения пустого состояния.

**Ключевые возможности**:
- Центрированный layout
- Кастомная иконка
- Заголовок и описание
- Действие (кнопка)

**Props**:
```typescript
interface EmptyStateProps {
  icon?: ReactNode;              // Иконка
  title: string;                 // Заголовок
  description?: string;          // Описание
  action?: ReactNode;            // Действие (кнопка)
}
```

**Оптимизация**: React.memo

---

### ⚙️ ActionMenu

**Файл**: `ActionMenu.tsx` (173 строки)

**Назначение**: Меню действий с иконками и цветовой индикацией.

**Ключевые возможности**:
- Иконки для элементов
- Цветовая индикация (error, warning)
- Disabled состояния
- Кастомная иконка кнопки
- Предотвращение всплытия событий

**Props**:
```typescript
interface ActionMenuProps {
  items: MenuItemConfig[];       // Элементы меню
  icon?: ReactNode;              // Кастомная иконка
}

interface MenuItemConfig {
  label: string;                 // Текст
  icon?: ReactNode;              // Иконка
  onClick: () => void;           // Обработчик
  disabled?: boolean;            // Неактивен
  color?: 'default' | 'error' | 'warning'; // Цвет
}
```

**Оптимизация**: useCallback для обработчиков

---

## Статистика

**Общее количество**:
- Компонентов: 9
- Строк кода: ~1,532
- TypeScript интерфейсов: 11+
- Документированных примеров: 9+

**Технологии**:
- React 18
- TypeScript
- Material-UI
- React Hook Form
- React.memo / useCallback / useMemo

**Покрытие функциональности**:
- ✅ Таблицы данных
- ✅ Пагинация
- ✅ Поиск и фильтрация
- ✅ Формы с валидацией
- ✅ Отображение статусов
- ✅ Загрузка и пустые состояния
- ✅ Меню действий

## Файлы документации

- `README.md` - Общее описание и примеры
- `USAGE.md` - Подробное руководство по использованию
- `COMPONENTS.md` (этот файл) - Справочник компонентов
- `example.tsx` - Полный пример использования

## Зависимости

```json
{
  "@mui/material": "^5.x",
  "@mui/icons-material": "^5.x",
  "react": "^18.x",
  "react-hook-form": "^7.x",
  "typescript": "^4.5+"
}
```

## Следующие шаги

Рекомендации по расширению библиотеки:

1. **Organisms** - создать сложные композиции:
   - UserTable (DataTable + SearchField + Pagination + ActionMenu)
   - LoginForm (FormField + LoadingOverlay)
   - DashboardCard (EmptyState + StatusChip)

2. **Дополнительные molecules**:
   - DateRangePicker
   - MultiSelect
   - FileUploader
   - ConfirmDialog
   - Toast/Snackbar

3. **Тестирование**:
   - Unit тесты (Jest + React Testing Library)
   - E2E тесты (Cypress)
   - Visual regression (Chromatic)

4. **Документация**:
   - Storybook для визуальной документации
   - Интерактивные примеры
   - API Reference

5. **Оптимизация**:
   - Code splitting
   - Tree shaking
   - Bundle size analysis
